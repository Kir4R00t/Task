services:
  flow-manager:
    build: .
    container_name: flow-manager
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    command: uvicorn flow_manager.api:app --host 0.0.0.0 --port 8000 --reload
    